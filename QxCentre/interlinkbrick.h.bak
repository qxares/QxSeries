#ifndef INTERLINKBRICK_H
#define INTERLINKBRICK_H

#include <QObject>
#include <QWidget>
#include <QList>

class InterlinkBrick : public QObject {
    Q_OBJECT
public:
    explicit InterlinkBrick(QWidget *parentWidget, QObject *parent = nullptr);
    void launchAppWindow(QWidget *window);
    void raiseAppWindows();

private:
    QWidget *parentWidget;
    QList<QWidget*> appWindows; // Track all child app windows
};

#endif // INTERLINKBRICK_H
